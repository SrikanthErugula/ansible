- name: functions
  hosts: local
  connection: local
  vars:
    name: " Srikanth reddy"
  tasks:
  - name: convert uppercase
    ansible.builtin.debug:
      msg: "{{ sriname | upper }}"

  # - name: convert into lowercase
  #   ansible.builtin.debug:
  #     msg: "{{ gmail | lower }}"