# - name: Print the vars_files
#   hosts: cart 
#   vars: 
#     SRI: kanth
#   tasks: 
#   - name: defined the vars
#     ansible.builtin.debug:
#       msg: "var sri is {{ SRI }}"

# - name: loops
#   hosts: local 
#   connection: local 
#   tasks:
#   - name: print the names
#     ansible.builtin.debug:
#       msg: " Hi {{ item }}"
#     loop:
#     - written
#     - dfg 

# - name: install pacakges
#   hosts: local 
#   connection: local 
#   become: yes
#   tasks: 
#   - name: install by using the loop process
#     ansible.builtin.package:
#       name: "{{ item }}"
#       state: latest
#     loop:
#     - nginx

# - name: functions
#   hosts: local
#   connection: local
#   vars:
#     fruits: apple,banana,orange,mango,pineapple,guava,custardapple # these are all strings
#     # sriname: " Erugula Srikanth reddy"
#     # gmail: Info@Gmail.com
#     # ipaddress: "19222.168.0.1"
#     # dictionary or map both are same
#     tags:
#       Application: payment
#       Environment: dev
#      # list:
#     listtags:
#       - key: Application
#         value: payment
#       - key: Environment
#         value: dev
#   tasks: 
#   - name: default variable
#     ansible.builtin.debug:
#       # msg: " Hello, {{ name }}" # error is : "The task includes an option with an undefined variable.
#       msg: " tags are {{ tags }}, listtags {{ listtags}}" #  so here under name not avilable at any value we need to use this cmd 

#       #######################################################



# - name: functions
#   hosts: local
#   connection: local
#   tasks: 
#   - name: default vars
#     ansible.builtin.debug:
#       msg: " hello {{ name| default('Srikanth') }}" # ERROR: {"msg": "The task includes an option with an undefined variable. The error was: 'name' is undefined. 'name .... so here in ansible  vars ni use chestu ekkada define cheyakapothe..... we can use default word in msg lo like msg: " hello {{ name | default('Srikanth') }}" .... mandatory ani pedithe we can definetely call vars
#       msg: " hello {{ name | mandatory }}" # FAILED! => {"msg": "Mandatory variable 'name'  not defined."}

# 01/03/26 Practice

# - name: variables from prompt
#   hosts: local
#   connection: local
#   vars_prompt: # this is syntax
#   - name: username
#     prompt: What is your username?
#     private: true # false ani vunte enter chestunte name visible avuthundhi..... True vunte after excute it will be visible...
#   - name: password # so here will get error  see in notes
#     prompt: what is your password
#     private: true # so here false ani vunte enter chesetappudu visible avuthundhi, adhe true ani vunte wont visible
#   tasks: 
#   - name: print credentials
#     ansible.builtin.debug:
#       msg: "Hello, Username is: {{ username }} Password is: {{ password }}"



- name: pwd
  hosts: local
  connection: local
  vars_prompt:
  - name: username
    prompt: what is ur name
    private: false
























