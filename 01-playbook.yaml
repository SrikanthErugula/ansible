- name: "ping the server" # ikkada next buttion click 
  hosts: frontend 
  # {so ikkada .ini folder loki velli frontend ane grp vunte dhani kindha vunna all ips of server ni 
  #excute chestundhi...... Which server you are connecting to anedhi hostes cheptundhi.}
  
  # List of tasks/collections/modules
  tasks: # so here antha same place lo one by one vundali
  - name: ping
    ansible.builtin.ping: # see in notes

  - name: "Printing first CMD"
    ansible.builtin.debug:
     msg: "Hi, this my first cmd"
###################################################

# {so here before : we callled keys.... and after : we called as values... so manam key anevi change 
#cheyalemu but we have a chance to change values }